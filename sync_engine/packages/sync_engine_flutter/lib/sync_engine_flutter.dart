/// STUB. Flutter bindings for the hellsyncie sync engine.
///
/// Later builds add: app-lifecycle-driven sync (foreground/background),
/// path_provider-backed local storage, and ChangeNotifier/Stream surfaces for
/// widgets. Intentionally empty and Flutter-free for now.
library;
