/// STUB. A [Backend] over a plain directory (dart:io).
///
/// Later builds implement list/download/upload/delete against a folder — local
/// disk, or a desktop-synced folder (Dropbox, Google Drive, iCloud). Must honor
/// the Backend contract's tolerances: atomic writes, no rewrites of another
/// device's files.
library;
